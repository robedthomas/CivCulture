<UserControl x:Class="CivCulture.Views.TechnologyBubbleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CivCulture.Views"
             xmlns:conv="clr-namespace:CivCulture.Utilities.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <d:Ellipse Width="90" Height="90" Stroke="SteelBlue" StrokeThickness="2" Fill="DarkSlateBlue"/>
        <d:Ellipse Width="15" Height="15" Margin="85,0,0,85" Fill="Green" />
        <Ellipse Width="90" Height="90" Stroke="{Binding ResearchState, Converter={conv:TechResearchStateToPerimeterStrokeConverter}}" StrokeThickness="2" Fill="{Binding ResearchState, Converter={conv:TechResearchStateToFillConverter}}"/>
        <Ellipse Width="15" Height="15" Margin="85,0,0,85" Fill="{Binding TechCategory, Converter={conv:TechCategoryToFillConverter}}" />
    </Grid>
</UserControl>
